---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: featureflags.platform.kratix.io
spec:
  group: platform.kratix.io
  names:
    kind: FeatureFlag
    listKind: FeatureFlagList
    plural: featureflags
    singular: featureflag
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: FeatureFlag is the Schema for the FeatureFlag API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              defaultEnabled:
                description: |-
                  DefaultEnabled indicates if the feature flag is enabled by default (i.e. the
                  behaviour for destinations that don't match any selectors).
                type: boolean
              description:
                description: Description of the feature flag
                type: string
              destinationSelectors:
                description: |-
                  DestinationSelectors is a list of selectors that allows toggling features per
                  destination.
                  Any destination that matches any of the selectors will have the feature flag
                  enabled according to the Enabled field in the selector.
                  Any destination that doesn't match any of the selectors will have the feature flag
                  enabled according to the DefaultEnabled field.
                items:
                  properties:
                    enabled:
                      description: Enabled indicates if the feature flag is enabled
                      type: boolean
                    matchLabels:
                      additionalProperties:
                        type: string
                      type: object
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}

apiVersion: platform.kratix.io/v1alpha1
kind: ResourceBinding
metadata:
  labels:
    app.kubernetes.io/name: kratix
    app.kubernetes.io/managed-by: kustomize
  name: resourcebinding-sample
spec:
  # TODO(user): Add fields here

apiVersion: platform.kratix.io/v1alpha1
kind: PromiseRevision
metadata:
  labels:
    app.kubernetes.io/name: kratix
    app.kubernetes.io/managed-by: kustomize
  name: promiserevision-sample
spec:
  # TODO(user): Add fields here

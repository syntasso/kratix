# Optional: apply the change for real, after previewing it with 07-dry-run-update.yaml.
#
# Identical to the `resource` block of 07-dry-run-update.yaml. Applying this closes
# the loop -- what the dry run predicted is what the real run produces. Compare the
# App's live Works afterwards against the diff the dry run showed.
apiVersion: example.kratix.io/v1alpha1
kind: App
metadata:
  name: demo-app
  namespace: default
spec:
  image: nginx:1.27
  replicas: 3
  tier: gold
  host: demo-v2.example.com
  tls: true
  dbVersion: "16"
  dbStorageGb: 20

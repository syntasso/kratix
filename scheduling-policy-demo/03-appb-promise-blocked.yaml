apiVersion: platform.kratix.io/v1alpha1
kind: Promise
metadata:
  name: appb
spec:
  destinationSelectors:
    - matchLabels:
        environment: dev
  api:
    apiVersion: apiextensions.k8s.io/v1
    kind: CustomResourceDefinition
    metadata:
      name: appbs.example.kratix.io
    spec:
      group: example.kratix.io
      names:
        kind: Appb
        plural: appbs
        singular: appb
      scope: Namespaced
      versions:
        - name: v1alpha1
          served: true
          storage: true
          schema:
            openAPIV3Schema:
              type: object
              properties:
                spec:
                  type: object
                  properties:
                    message:
                      type: string
  workflows:
    promise:
      configure:
        - apiVersion: platform.kratix.io/v1alpha1
          kind: Pipeline
          metadata:
            name: appb-promise-configure
          spec:
            containers:
              - name: configure
                image: busybox:1.36
                command: ["sh", "-c"]
                args:
                  - |
                    cat <<'EOF' > /kratix/output/configmap.yaml
                    apiVersion: v1
                    kind: ConfigMap
                    metadata:
                      name: appb-promise-config
                      namespace: default
                    data:
                      message: "appb promise-level workload"
                    EOF
                securityContext:
                  runAsUser: 1000
                  runAsNonRoot: true
                  allowPrivilegeEscalation: false
                  capabilities:
                    drop: ["ALL"]
                  seccompProfile:
                    type: RuntimeDefault

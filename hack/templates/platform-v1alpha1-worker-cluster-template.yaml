apiVersion: platform.kratix.io/v1alpha1
kind: Destination
metadata:
  name: DESTINATION_NAME
  labels:
    environment: ENVIRONMENT_VALUE
    data: cache
spec:
  path: PATH
  stateStoreRef:
    kind: KIND GitStateStore/BucketStateStore
    name: NAME

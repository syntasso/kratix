// Package perf hosts the controller-performance test rig.
//
// The rig is build-tagged (perf) so it is excluded from the default
// `go test ./...` and `make test` runs. Invoke via `make perf-test`.
// See test/perf/README.md for the full how-to.
package perf

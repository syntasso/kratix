apiVersion: test.kratix.io/v1alpha1
kind: bash
metadata:
  name: example-rr
spec:
  container0Cmd: |
    kubectl create namespace declarative-$(yq '.metadata.name' /kratix/input/object.yaml)-v1alpha2 --dry-run=client -oyaml > /kratix/output/namespace.yaml
  container1Cmd: |
    echo "done"

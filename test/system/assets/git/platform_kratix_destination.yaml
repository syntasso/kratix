---
apiVersion: platform.kratix.io/v1alpha1
kind: Destination
metadata:
  name: platform-cluster
  labels:
    environment: platform
spec:
  path: bar/baz
  stateStoreRef:
    name: system-test
    kind: GitStateStore

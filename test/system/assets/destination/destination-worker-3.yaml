apiVersion: platform.kratix.io/v1alpha1
kind: Destination
metadata:
  name: worker-3
  labels:
    target: destination-status-bucket
spec:
  strictMatchLabels: true
  path: worker-3
  stateStoreRef:
    name: destination-test-store
    kind: BucketStateStore
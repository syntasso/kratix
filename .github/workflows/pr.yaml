name: Test Kratix

on:
  pull_request:
    branches: [ "main" ]

jobs:
  run-tests:
    #uses: syntasso/ci/.github/workflows/test-kratix.yaml@main
    uses: syntasso/ci/.github/workflows/test-kratix.yaml@2aadd2c6cd148fd50ab72c809a2ddf68ace9c742
    secrets: inherit
    with:
      sha: ${{ github.event.pull_request.head.sha }}
      message: ${{ github.event.pull_request.title }}

name: Test Kratix

on:
  pull_request:
    branches: [ "main" ]


jobs:
  run-tests:
    #uses: syntasso/ci/.github/workflows/test-kratix.yaml@main
    uses: syntasso/ci/.github/workflows/test-kratix.yaml@941ad9a34fc944f7a2b35797ed30536322113b8b
    secrets: inherit
    with:
      sha: ${{ github.event.pull_request.head.sha }}
      message: ${{ github.event.pull_request.title }}

apiVersion: platform.kratix.io/v1alpha1
kind: FeatureFlag
metadata:
  name: write-example-manifests-to-destinations
spec:
  description: "Whether to write example canary manifests to Destinations."
  destinationSelectors:
    - matchLabels:
        environment: dev
      enabled: false
  defaultEnabled: true

TODO:
* Check with Stuart if he is using our quick start (or applying our yamls, inc
	minio), because otherwise it will fail
* Remove startup flag for the repository type

---
apiVersion: platform.kratix.io/v1alpha1
kind: Cluster
metadata:
  name: foo
	namespace: default
spec:
  stateStoreRef:
	  name
		namespace # optional: default is cluster .metadata.namespace
  path # optional: default is cluster .metadata.name
---
apiVersion: platform.kratix.io/v1alpha1
# based on: https://github.com/open-gitops/documents/blob/v0.1.0/PRINCIPLES.md#state-store
kind: StateStore
spec:
	secretRef:
		name
		namespace # optional: default is cluster .metadata.namespace
	# git:
	#   url
	#   ref
	s3:
		name
		endpoint
		useSSL # optional: default true
---
apiVersion: v1
kind: Secret
metadata: ...
spec:
  accessKeyID
	secretAccessKey

/*** ***/

kratix-platform-system
	kratix
		cluster controller
		promise controller
	minio

default
	secret:
		cluster-foo
	statestore:
		foostore
		secretRef: cluster-foo
	cluster
		stateRef: foostore
